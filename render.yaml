services:
  - type: web
    name: salary-predictor-api
    env: python
    buildCommand: pip install -e .
    startCommand: uvicorn salary_predictor.api.main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.13
